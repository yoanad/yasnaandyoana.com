import React from 'react';
import './Hero.css';

const Hero = () => {
    return (
        <section className="hero">
            {/* <img src={hero} alt="Soaps" /> */}
        </section>
    );
}

export default Hero;