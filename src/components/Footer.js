import React from 'react';
import footer from '../assets/footer.png'; // Assuming the footer image file is in the './assets' directory

const Footer = () => {
    return (
        <footer>
            <img src={footer} alt="Footer" />
        </footer>
    );
}

export default Footer;
